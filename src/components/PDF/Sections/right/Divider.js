import { View } from '@react-pdf/renderer'

const Divider = () => (
  <View
    style={{
      height: '1px',
      marginTop: '4px',
      marginBottom: '8px',
      width: '100%',
      backgroundColor: 'grey',
    }}
  ></View>
)

export default Divider
